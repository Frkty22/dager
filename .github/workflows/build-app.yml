name: Build Windows Application

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4 # تم تحديث هذا أيضًا للإصدار الأحدث
        with:
          lfs: true

      - name: Set up Python 3.10
        uses: actions/setup-python@v5 # تم تحديث هذا أيضًا للإصدار الأحدث
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run PyInstaller
        run: pyinstaller DarkForge_Master_Pipeline.spec

      - name: Upload artifact
        uses: actions/upload-artifact@v4 # هذا هو التعديل الأهم
        with:
          name: windows-application
          path: dist/
